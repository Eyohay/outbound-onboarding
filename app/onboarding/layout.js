export const metadata = {
  title: "Client Onboarding — Outbound Consulting",
  description: "Complete your onboarding form before your kickoff call.",
};

export default function OnboardingLayout({ children }) {
  return children;
}
